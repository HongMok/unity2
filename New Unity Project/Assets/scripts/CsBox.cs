using UnityEngine;
using System.Collections;


public class CsBox : MonoBehaviour {

		public Rigidbody ballTemp;
		public Rigidbody ball;

	// Use this for initialization
	void Start () {

//				ball = Instantiate (ballTemp, Vector3 (0, 1, 0), ballTemp.rotation);
//				ball.renderer.material.mainTexture = Resources.Load ("风景1", Texture2D);

		
//				ball.velocity = Vector3 (-8, 0, -15);
				Debug.Log ("hello");
//				ball.transform.position = Vector3 (0, 1, 0);
//		ball.renderer.material.mainTexture = Resources.Load ("风景1") as Texture;

//		ball.velocity = Vector3(-8, 0, -15);

	}
	
	// Update is called once per frame
	void Update () {

	
	}
}
